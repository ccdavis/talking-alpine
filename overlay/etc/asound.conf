# Card 0 by default; speech-audio-next rewrites this to pick another card.
# The `default` PCM goes through dmix, so tdsr and espeakup (Speakup) can
# both play.
defaults.pcm.card 0
defaults.ctl.card 0
defaults.pcm.dmix.rate 48000
