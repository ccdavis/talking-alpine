The repository index on the stick (`apks/x86_64/APKINDEX.tar.gz`) is signed with
`talkalpine.rsa`, which is git-ignored and generated by `run/mkimage.sh` when
missing. Its public half is shipped in the overlay (`/etc/apk/keys`). A new key
only means the next image trusts a different signer; nothing else depends on it.
